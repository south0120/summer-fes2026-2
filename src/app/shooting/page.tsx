import GamePlaceholder from "@/components/GamePlaceholder";

export const metadata = { title: "射的 | Substack 夏祭り" };

export default function ShootingPage() {
  return (
    <GamePlaceholder title="射 的" emoji="🎯" desc="的をねらって景品をゲット！" />
  );
}
